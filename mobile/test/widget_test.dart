import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Fleet Mint app smoke test', (WidgetTester tester) async {
    // Placeholder — full tests require a running backend
    expect(1 + 1, equals(2));
  });
}
